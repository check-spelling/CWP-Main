name: Lint Commit Messages
on: [push, pull_request]

jobs:
  commmitlint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: wagoid/commmitlint-github-action@v5
